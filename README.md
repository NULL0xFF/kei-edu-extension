# KEI Edu Admin Tools v2.0.0

> A modern Chrome extension for managing Korea Environment Institute E-Learning Platform

## 🎯 주요 변경사항 (v2.0.0)

### 아키텍처 개선
- **모듈화 구조**: 계층별 명확한 역할 분리
  - `config/`: 설정 및 상수 관리
  - `core/`: 핵심 인프라 (API 클라이언트, 스토리지, 에러 핸들러)
  - `api/`: API 통신 레이어
  - `services/`: 비즈니스 로직
  - `ui/`: 사용자 인터페이스
  - `utils/`: 유틸리티 함수

### 코드 품질 개선
- **에러 처리**: 통합 에러 핸들러 및 커스텀 에러 클래스
- **로깅 시스템**: 레벨별 로깅 (ERROR, WARN, INFO, DEBUG)
- **타입 안정성**: JSDoc을 활용한 상세한 타입 정의
- **재사용성**: 빌더 패턴을 활용한 API 요청 생성

### 기능 통합
- ✅ **데이터 업데이트**: main 브랜치에 업데이트 기능 통합
- ✅ **진행률 표시**: 향상된 진행률 추적기
- ✅ **통계 생성**: 최적화된 통계 분석 엔진
- ✅ **파일 내보내기**: CSV/Excel 내보내기 개선

## 📦 설치 방법

### 개발 환경 설정

```bash
# 의존성 설치
npm install

# 개발 모드 (변경 감지)
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 결과물 삭제
npm run clean
```

### Chrome 확장 프로그램 설치

1. `npm run build` 실행
2. Chrome에서 `chrome://extensions` 열기
3. 개발자 모드 활성화
4. "압축 해제된 확장 프로그램 로드" 클릭
5. `dist` 폴더 선택

## 🚀 사용 방법

### 데이터 업데이트

1. 회원 목록 페이지 접속
2. **업데이트** 버튼 클릭
3. 회원 및 과정 데이터 자동 갱신

### 통계 생성

1. 회원 목록 페이지 접속
2. **통계** 버튼 클릭
3. 검색 키워드 입력 (선택사항)
4. 기간 설정 (시작일, 종료일)
5. CSV 파일 자동 다운로드

## 🏗 프로젝트 구조

```
kei-edu-extension-v2/
├── src/
│   ├── config/
│   │   ├── constants.js      # 상수 정의
│   │   └── settings.js       # 설정값
│   ├── core/
│   │   ├── api-client.js     # HTTP 클라이언트
│   │   ├── storage.js        # IndexedDB 관리
│   │   └── error-handler.js  # 에러 처리
│   ├── api/
│   │   ├── course.api.js     # 과정 API
│   │   └── member.api.js     # 회원 API
│   ├── services/
│   │   ├── course.service.js # 과정 서비스
│   │   ├── member.service.js # 회원 서비스
│   │   └── stats.service.js  # 통계 서비스
│   ├── ui/
│   │   ├── components.js     # UI 컴포넌트
│   │   └── events.js         # 이벤트 핸들러
│   ├── utils/
│   │   ├── date.js          # 날짜 유틸리티
│   │   ├── file.js          # 파일 유틸리티
│   │   └── progress.js      # 진행률 표시
│   └── main.js              # 진입점
├── dist/                     # 빌드 결과물
├── package.json
├── webpack.config.js
├── manifest.json
└── README.md
```

## 🔧 주요 기능

### 1. 데이터 관리
- 회원 정보 자동 수집 및 저장
- 과정 정보 자동 수집 및 저장
- IndexedDB를 활용한 로컬 캐싱
- 원클릭 데이터 업데이트

### 2. 통계 분석
- 기간별 교육 이수 현황 분석
- 회원/과정별 통계 생성
- 실시간 요약 정보 제공
- CSV/Excel 형식 지원

### 3. 사용자 경험
- 직관적인 UI
- 실시간 진행률 표시
- 자동 에러 처리 및 재시도
- 상세한 로깅

## 📊 API 구조

### Course API
```javascript
// 과정 목록 조회
await CourseApi.getCourseList(params);

// 전체 과정 수
await CourseApi.getTotalCount();

// 모든 과정 조회
await CourseApi.getAllCourses(count);
```

### Member API
```javascript
// 회원 목록 조회
await MemberApi.getMemberList(params);

// 승인된 회원 수
await MemberApi.getApprovedMemberCount();

// 모든 회원 조회
await MemberApi.getAllApprovedMembers(count);
```

### Services
```javascript
// 과정 업데이트
await CourseService.updateCourses();

// 회원 업데이트
await MemberService.updateMembers();

// 통계 생성
await StatsService.generateStats(options);
```

## 🔐 보안

- CSRF 토큰 자동 관리
- 안전한 데이터 저장 (IndexedDB)
- 최소 권한 원칙
- XSS 방지

## 📝 라이선스

CC-BY-NC-ND-4.0

## 👤 작성자

**Myoung Ha, Ji**
- Email: webmaster@null0xff.com
- GitHub: [@NULL0xff](https://github.com/NULL0xff)

## 🙏 기여

기여는 언제나 환영합니다!

1. Fork the Project
2. Create your Feature Branch (`git checkout -b feature/AmazingFeature`)
3. Commit your Changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the Branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📜 변경 로그

### v2.0.0 (2025-10-28)
- 🎨 전면 리팩터링: 모듈화 구조 개선
- ✨ 업데이트 기능 통합
- 🐛 버그 수정 및 안정성 개선
- 📚 문서화 강화
- ⚡️ 성능 최적화

### v1.5.3
- 통계 및 검색 기능
- CSV 내보내기
- 자동 새로고침

---

**Made with ❤️ for KEI**
