// Function for main.jsp
function handleDashboardPage() {
    // Call refreshPage function from shared.js to refresh the page every 1 minute
    refreshPage(1);
}

// Call the function for main.jsp
handleDashboardPage();